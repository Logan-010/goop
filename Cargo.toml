[workspace]
resolver = "2"
members = [ "kad-redb", "goop-cli" ]

[workspace.dependencies]
kad-redb = { path = "kad-redb" }

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
debug = false
incremental = false
rpath = false
overflow-checks = false
